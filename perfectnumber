#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,r,sum=0;
    cin>>n;
    for(int i=1;i<n;i++)
    {
        r=n%i;
        if(r==0)
        {
            sum=sum+i;
        }
    }
    if(sum==n)
    cout<<"True";
    else
    cout<<"False";
}
