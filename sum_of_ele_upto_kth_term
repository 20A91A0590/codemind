#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    int a[n],i,k,sum=0;;
    for(i=0;i<n;i++)
    {
        cin>>a[i];
    }
   cin>>k;
   for(i=0;i<k;i++)
   {
       sum=sum+a[i];
   }
   cout<<sum;
}
