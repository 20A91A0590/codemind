#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,sum=0,r,p=1;
    cin>>n;
    while(n>0)
    {
        r=n%10;
        sum=sum+r;
        p=p*r;
        n=n/10;
    }
    if(sum==p)
    cout<<"Spy Number";
    else  
    cout<<"Not Spy Number ";
    return 0;
}
