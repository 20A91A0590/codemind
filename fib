#include <bits/stdc++.h>
using namespace std;
int main() {

  int i, n;
  cin>>n;
  int a = 0, b = 1;
  cout<<a<<" "<<b;
  int c = a + b;
  for (i = 3; i <= n; ++i) {
    cout<<" "<<c;
    a = b;
    b = c;
    c = a + b;
  }

  return 0;
}
